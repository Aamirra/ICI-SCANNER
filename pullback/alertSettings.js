// ─────────────────────────────────────────
// alertSettings.js
// Kaam: Alert ki saari fixed settings yahan hain
// ─────────────────────────────────────────

const MAX_ALERT_CACHE = 500;          // Kitne alerts cache mein rakho
const REMINDER_DELAY_MS = 30 * 60 * 1000; // 30 min baad reminder

module.exports = { MAX_ALERT_CACHE, REMINDER_DELAY_MS };
